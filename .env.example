# GitForAI Configuration
# Copy this file to .env and fill in your values

# Embedding Provider (Phase 2 - Semantic enrichment)
# Default: "local" (free, privacy-preserving, offline, no API key needed)
# Optional: "openai" (~5-7% better quality, requires API key, costs ~$0.02/1M tokens)
EMBEDDING_PROVIDER=local

# LLM API Keys (Optional - Only required for OpenAI provider)
# Leave commented out if using local embeddings (default)
# OPENAI_API_KEY=sk-your-openai-api-key-here
# ANTHROPIC_API_KEY=sk-ant-your-anthropic-key-here

# LLM Settings (Provider-specific)
# For local provider:
#   - EMBEDDING_MODEL: all-MiniLM-L6-v2 (default, 384 dims, 80MB)
#   - Alternative models: all-mpnet-base-v2, paraphrase-MiniLM-L6-v2, bge-small-en-v1.5
# For openai provider:
#   - LLM_MODEL: gpt-4-turbo-preview (for completions)
#   - EMBEDDING_MODEL: text-embedding-3-small (1536 dims)
LLM_MODEL=gpt-4-turbo-preview
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Vector Database Settings (Phase 3 - Optional for now)
VECTORDB_PROVIDER=chroma
VECTORDB_PERSIST_DIR=./chroma_data
VECTORDB_COLLECTION=git_history

# Processing Settings
BATCH_SIZE=10
MAX_WORKERS=4
ENABLE_CACHING=true
CACHE_DIR=./.cache

# Logging
LOG_LEVEL=INFO
